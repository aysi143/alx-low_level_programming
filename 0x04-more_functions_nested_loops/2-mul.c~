#include "holberton.h"
/**
 * mul - multiplies two integers
 * @a: multiplying
 * @b: multiplier
 *
 * Return: the result of mutiplication
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
